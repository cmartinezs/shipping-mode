# S6 Integrated Prototype Evidence

Result: PASSED.

The prototype demonstrates the complete vertical-slice sequence, a hashed
ChangeSet, an event journal, a successful check/report projection, and hook
protection against direct `.planning/**` writes.

```bash
node spikes/integrated-prototype/tests/integrated-prototype.test.mjs
```
