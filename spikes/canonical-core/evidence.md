# S3 Canonical Core Evidence

Result: PASSED.

The prototype proves deterministic serialization and hashing, UUIDv7 identity,
canonical aggregate paths, and the closed DSL operator set.

```bash
node spikes/canonical-core/tests/canonical-core.test.mjs
```
