diff --git a/runtime/src/lib/releasePolicy.mjs b/runtime/src/lib/releasePolicy.mjs
index 6ed5e8e..0000000 100644
--- a/runtime/src/lib/releasePolicy.mjs
+++ b/runtime/src/lib/releasePolicy.mjs
@@ -58,11 +58,20 @@ function releaseRecordMap(releases, targetRelease = null, nextPolicy = null, nex
   const records = new Map();
   for (const release of releases) records.set(release.id, release);
   if (targetRelease) {
+    const candidateWithoutRevision = {
+      ...targetRelease,
+      lane: { id: nextLaneId },
+      policy: nextPolicy,
+      audit: { ...targetRelease.audit }
+    };
+    delete candidateWithoutRevision.audit.revision;
     records.set(targetRelease.id, {
-      ...targetRelease,
-      lane: { id: nextLaneId },
-      policy: nextPolicy
+      ...candidateWithoutRevision,
+      audit: {
+        ...candidateWithoutRevision.audit,
+        revision: `sha256:${revisionHash(candidateWithoutRevision)}`
+      }
     });
   }
   return records;
 }

diff --git a/runtime/src/lib/releaseMutations.mjs b/runtime/src/lib/releaseMutations.mjs
index 984645f..0000000 100644
--- a/runtime/src/lib/releaseMutations.mjs
+++ b/runtime/src/lib/releaseMutations.mjs
@@ -115,7 +115,27 @@ export function releaseMutationInvariantFindings(changeSet, operation = null, pl
 
   const actor = operation?.proposedBy ?? payload.updatedBy;
   try {
-    const normalized = normalizeReleaseMutationRequest(changeSet.kind, { ...snapshot, idempotencyKey: payload.idempotencyKey }, {
+    const rawRequest = { releaseRef: snapshot.releaseRef, idempotencyKey: payload.idempotencyKey };
+    if (changeSet.kind === "release.policy.configure") {
+      if (snapshot.laneId !== null) rawRequest.laneId = snapshot.laneId;
+      if (snapshot.policyMode !== null) rawRequest.policyMode = snapshot.policyMode;
+      if (snapshot.previousReleaseRefs !== null) rawRequest.previousReleaseRefs = snapshot.previousReleaseRefs;
+      if (snapshot.dependencyRefs !== null) rawRequest.dependencyRefs = snapshot.dependencyRefs;
+    } else if (changeSet.kind === "release.scopeRefs.set") {
+      rawRequest.scopeIds = snapshot.scopeIds;
+      rawRequest.policyMode = snapshot.policyMode;
+    } else if (changeSet.kind === "release.operationalRefs.set") {
+      rawRequest.executionContextRefs = snapshot.executionContextRefs;
+      rawRequest.environmentRefs = snapshot.environmentRefs;
+    } else if (changeSet.kind === "release.deployment.record") {
+      rawRequest.environmentRef = snapshot.environmentRef;
+      rawRequest.executionContextRef = snapshot.executionContextRef;
+      rawRequest.status = snapshot.status;
+      rawRequest.artifactRefs = snapshot.artifactRefs;
+      rawRequest.evidenceRefs = snapshot.evidenceRefs;
+      rawRequest.completedAt = snapshot.completedAt;
+    }
+    const normalized = normalizeReleaseMutationRequest(changeSet.kind, rawRequest, {
       actor,
       defaultIdempotencyKey: payload.idempotencyKey
     });

diff --git a/docs/superpowers/plans/2026-07-29-corte-2-plan-2-release-policy-operational-refs.md b/docs/superpowers/plans/2026-07-29-corte-2-plan-2-release-policy-operational-refs.md
index f8c4989..0000000 100644
--- a/docs/superpowers/plans/2026-07-29-corte-2-plan-2-release-policy-operational-refs.md
+++ b/docs/superpowers/plans/2026-07-29-corte-2-plan-2-release-policy-operational-refs.md
@@ -493,4 +493,7 @@ Adversarial review of PR #22 found and closed two trust-boundary gaps:
 - Release Plan 2 Operations now retain a server-owned request binding and proposal hash. Editing a persisted ChangeSet and recomputing its public hash cannot change caller intent, actor, timestamps, generated deployment identity or resolved Release targets before validation.
 - Release policy validation now evaluates the complete Release catalog. Reconfiguring a sequence root or predecessor cannot orphan existing successors, create multiple roots, introduce branching or hide a cycle. `check schema` reports these global policy inconsistencies.
 
+- Candidate Release policy states receive a recomputed canonical revision before full-catalog graph validation, so incoming references and indirect cycles are evaluated against the actual proposed state instead of synthetic revision drift.
+- Snapshot revalidation preserves the semantic distinction between omitted policy fields (`null`) and explicitly requested empty reference lists.
+
 The corrections preserve the Plan 2 boundary and do not add lifecycle transitions, deployment execution, Release Items, Work Packages, Tasks, final readiness or finalization.
