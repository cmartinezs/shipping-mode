# ADR-0003: Canonical identity and paths

Canonical metadata is serialized with recursively sorted object keys and
hashed with SHA-256. Aggregate directories use UUIDv7 primary IDs; display IDs
and slugs remain projections. The executable DSL accepts only the documented
closed operator set.
